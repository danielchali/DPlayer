<Window x:Class="DPlayer.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DPlayer.App"
        xmlns:vlc="clr-namespace:LibVLCSharp.WPF;assembly=LibVLCSharp.WPF"
        mc:Ignorable="d"
        Title="DPlayer" Height="600" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="#1e1e1e"
        Icon="Dplayer.ico"
        PreviewMouseMove="MainWindow_MouseMove">
    <Window.Resources>
        <!-- Dark ContextMenu Style -->
        <Style TargetType="ContextMenu">
            <Setter Property="Background" Value="#222222"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#444444"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <!-- Dark MenuItem Style -->
        <Style TargetType="MenuItem">
            <Setter Property="Background" Value="#222222"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>

        <!-- Dark ToggleButton Style for ComboBox -->
        <Style x:Key="ComboBoxToggleButton" TargetType="ToggleButton">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="Border" 
                                Background="{Binding Background, RelativeSource={RelativeSource TemplatedParent}}" 
                                BorderBrush="{Binding BorderBrush, RelativeSource={RelativeSource TemplatedParent}}" 
                                BorderThickness="{Binding BorderThickness, RelativeSource={RelativeSource TemplatedParent}}" 
                                CornerRadius="3">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition />
                                    <ColumnDefinition Width="20" />
                                </Grid.ColumnDefinitions>
                                <Path x:Name="Arrow"
                                      Grid.Column="1"     
                                      Fill="{Binding Foreground, RelativeSource={RelativeSource TemplatedParent}}"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center"
                                      Data="M 0 0 L 3.5 3.5 L 7 0 Z"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#4d4d4d" />
                                <Setter TargetName="Border" Property="BorderBrush" Value="#666666" />
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#2d2d2d" />
                                <Setter TargetName="Border" Property="BorderBrush" Value="#555555" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Border" Property="Background" Value="#222222" />
                                <Setter TargetName="Border" Property="BorderBrush" Value="#333333" />
                                <Setter TargetName="Arrow" Property="Fill" Value="#555555" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Dark ComboBox Style -->
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#2d2d2d"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#444444"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.CanContentScroll" Value="true"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton Name="ToggleButton" 
                                          Style="{StaticResource ComboBoxToggleButton}" 
                                          Focusable="false"
                                          Background="{TemplateBinding Background}"
                                          Foreground="{TemplateBinding Foreground}"
                                          BorderBrush="{TemplateBinding BorderBrush}"
                                          BorderThickness="{TemplateBinding BorderThickness}"
                                          IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press"/>
                            <ContentPresenter Name="ContentSite"
                                              IsHitTestVisible="False" 
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              Margin="8,3,23,3"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left"
                                              TextElement.Foreground="{TemplateBinding Foreground}"/>
                            <Popup Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True" 
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                <Grid Name="DropDown"
                                      SnapsToDevicePixels="True"                
                                      MinWidth="{TemplateBinding ActualWidth}"
                                      MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder"
                                            Background="#2d2d2d"
                                            BorderThickness="1"
                                            BorderBrush="#555555"
                                            CornerRadius="3"/>
                                    <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="HasItems" Value="false">
                                <Setter TargetName="DropDownBorder" Property="MinHeight" Value="95"/>
                            </Trigger>
                            <Trigger Property="IsGrouping" Value="true">
                                <Setter Property="ScrollViewer.CanContentScroll" Value="false"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Dark ComboBoxItem Style -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="#2d2d2d"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#4d4d4d"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#0078d4"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Dark Separator Style -->
        <Style TargetType="Separator">
            <Setter Property="Background" Value="#444444"/>
            <Setter Property="Margin" Value="0,2"/>
        </Style>

        <!-- Rounded Slider Style -->
        <Style x:Key="RoundedSliderStyle" TargetType="Slider">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Grid Background="Transparent">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto" MinHeight="{TemplateBinding MinHeight}"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <!-- Track Background -->
                            <Border x:Name="TrackBackground"
                                    Grid.Row="1"
                                    Height="6"
                                    CornerRadius="3"
                                    Background="#444444"
                                    VerticalAlignment="Center"/>

                            <!-- Active Track and Thumb -->
                            <Track x:Name="PART_Track" Grid.Row="1">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="{x:Static Slider.DecreaseLarge}">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <Border Height="6" CornerRadius="3,0,0,3" Background="#0078d4"/>
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="{x:Static Slider.IncreaseLarge}">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <Border Height="6" Background="Transparent"/>
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb x:Name="Thumb" Width="12" Height="12" VerticalAlignment="Center">
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Ellipse x:Name="ThumbEllipse" 
                                                         Width="12" 
                                                         Height="12" 
                                                         Fill="White" 
                                                         HorizontalAlignment="Center" 
                                                         VerticalAlignment="Center">
                                                    <Ellipse.Effect>
                                                        <DropShadowEffect BlurRadius="4" Color="Black" Opacity="0.3" ShadowDepth="1"/>
                                                    </Ellipse.Effect>
                                                </Ellipse>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="ThumbEllipse" Property="Fill" Value="#e0e0e0"/>
                                                        <Setter TargetName="ThumbEllipse" Property="Width" Value="14"/>
                                                        <Setter TargetName="ThumbEllipse" Property="Height" Value="14"/>
                                                    </Trigger>
                                                    <Trigger Property="IsDragging" Value="True">
                                                        <Setter TargetName="ThumbEllipse" Property="Fill" Value="#005a9e"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="TrackBackground" Property="Background" Value="#333333"/>
                                <Setter TargetName="PART_Track" Property="IsEnabled" Value="False"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid x:Name="RootGrid">
        <Grid.ContextMenu>
            <ContextMenu x:Name="PlayerContextMenu">
                <MenuItem Header="Open File..." Click="OpenFileButton_Click" />
                <MenuItem Header="Open URL..." Click="OpenUrlButton_Click" />
                <Separator />
                <MenuItem Header="Play / Pause" Click="PlayPauseButton_Click" />
                <MenuItem Header="Stop" Click="StopButton_Click" />
                <Separator />
                <MenuItem Header="Playback Speed">
                    <MenuItem Header="0.5x" Tag="0.5" Click="ContextMenuSpeed_Click" />
                    <MenuItem Header="0.75x" Tag="0.75" Click="ContextMenuSpeed_Click" />
                    <MenuItem Header="1.0x" Tag="1.0" Click="ContextMenuSpeed_Click" />
                    <MenuItem Header="1.25x" Tag="1.25" Click="ContextMenuSpeed_Click" />
                    <MenuItem Header="1.5x" Tag="1.5" Click="ContextMenuSpeed_Click" />
                    <MenuItem Header="2.0x" Tag="2.0" Click="ContextMenuSpeed_Click" />
                </MenuItem>
                <MenuItem Header="Volume">
                    <MenuItem Header="Mute / Unmute" Click="MuteButton_Click" />
                    <Separator />
                    <MenuItem Header="25%" Tag="25" Click="ContextMenuVolume_Click" />
                    <MenuItem Header="50%" Tag="50" Click="ContextMenuVolume_Click" />
                    <MenuItem Header="75%" Tag="75" Click="ContextMenuVolume_Click" />
                    <MenuItem Header="100%" Tag="100" Click="ContextMenuVolume_Click" />
                </MenuItem>
                <Separator />
                <MenuItem Header="Toggle Fullscreen" Click="FullScreenButton_Click" />
            </ContextMenu>
        </Grid.ContextMenu>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition x:Name="ControlsRow" Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Video Player Area -->
        <Grid Grid.Row="0" Background="#000000">
            <vlc:VideoView x:Name="VideoView" Visibility="Collapsed" />
            <StackPanel x:Name="NoMediaPanel" 
                        VerticalAlignment="Center" 
                        HorizontalAlignment="Center"
                        Visibility="Visible">
                <Image Source="Dplayerlogo.png" 
                       Width="120" 
                       Height="120" 
                       Margin="0,0,0,15"
                       HorizontalAlignment="Center"
                       RenderOptions.BitmapScalingMode="HighQuality"/>
                <TextBlock Text="DPlayer" 
                           FontSize="32" 
                           FontWeight="Bold"
                           Foreground="White" 
                           HorizontalAlignment="Center"
                           Margin="0,0,0,5"/>
                <TextBlock Text="No media loaded" 
                           FontSize="14" 
                           Foreground="#888888" 
                           HorizontalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" 
                            HorizontalAlignment="Center" 
                            Margin="0,25,0,0">
                    <Button x:Name="OpenFileButton" 
                            Content="Open File" 
                            Width="120" 
                            Height="35" 
                            Margin="5"
                            Click="OpenFileButton_Click"
                            Background="#3a3a3a" 
                            Foreground="White"
                            BorderBrush="#555555"
                            Cursor="Hand"/>
                    <Button x:Name="OpenUrlButton" 
                            Content="Open URL" 
                            Width="120" 
                            Height="35" 
                            Margin="5"
                            Click="OpenUrlButton_Click"
                            Background="#3a3a3a" 
                            Foreground="White"
                            BorderBrush="#555555"
                            Cursor="Hand"/>
                    <Button x:Name="LibraryButton" 
                            Content="Library" 
                            Width="120" 
                            Height="35" 
                            Margin="5"
                            Click="LibraryButton_Click"
                            Background="#3a3a3a" 
                            Foreground="White"
                            BorderBrush="#555555"
                            Cursor="Hand"/>
                </StackPanel>
            </StackPanel>

            <!-- Transparent overlay to capture mouse events over the native VLC HwndHost -->
            <Grid x:Name="MouseCaptureOverlay"
                  Background="Transparent"
                  MouseMove="MainWindow_MouseMove"
                  Visibility="Collapsed" />
        </Grid>

        <!-- Playback Controls (normal mode: Row 1; fullscreen: overlaid at bottom via VerticalAlignment) -->
        <Grid x:Name="PlaybackControls" Grid.Row="1" Background="#2d2d2d">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Playback Buttons -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <Button x:Name="StopButton" 
                        Content="⏹" 
                        Width="40" 
                        Height="30" 
                        Margin="0,0,5,0"
                        Click="StopButton_Click"
                        Background="Transparent" 
                        Foreground="White"
                        BorderBrush="Transparent"
                        FontSize="16"
                        Cursor="Hand"/>
                <Button x:Name="PreviousButton" 
                        Content="⏮" 
                        Width="40" 
                        Height="30" 
                        Margin="0,0,5,0"
                        Click="PreviousButton_Click"
                        Background="Transparent" 
                        Foreground="White"
                        BorderBrush="Transparent"
                        FontSize="16"
                        Cursor="Hand"/>
                <Button x:Name="PlayPauseButton" 
                        Content="▶" 
                        Width="45" 
                        Height="35" 
                        Margin="0,0,5,0"
                        Click="PlayPauseButton_Click"
                        Background="#0078d4" 
                        Foreground="White"
                        BorderBrush="Transparent"
                        FontSize="18"
                        Cursor="Hand"/>
                <Button x:Name="NextButton" 
                        Content="⏭" 
                        Width="40" 
                        Height="30" 
                        Margin="0,0,5,0"
                        Click="NextButton_Click"
                        Background="Transparent" 
                        Foreground="White"
                        BorderBrush="Transparent"
                        FontSize="16"
                        Cursor="Hand"/>
            </StackPanel>

            <!-- Progress Bar and Time -->
            <StackPanel x:Name="ProgressTimePanel" Grid.Column="1" VerticalAlignment="Center" Margin="10,0" Visibility="Collapsed">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" 
                               x:Name="CurrentTimeText" 
                               Text="00:00" 
                               Foreground="White" 
                               FontSize="12"
                               VerticalAlignment="Center"
                               Margin="0,0,10,0"/>
                    <Slider Grid.Column="1" 
                            x:Name="ProgressSlider" 
                            Style="{StaticResource RoundedSliderStyle}"
                            Minimum="0" 
                            Maximum="100" 
                            Value="0"
                            VerticalAlignment="Center"
                            PreviewMouseLeftButtonUp="ProgressSlider_PreviewMouseLeftButtonUp"
                            Visibility="Visible"/>
                    <TextBlock Grid.Column="2" 
                               x:Name="TotalTimeText" 
                               Text="00:00" 
                               Foreground="White" 
                               FontSize="12"
                               VerticalAlignment="Center"
                               Margin="10,0,0,0"/>
                </Grid>
            </StackPanel>

            <!-- Volume and Speed Controls -->
            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock x:Name="SpeedText" 
                           Text="1.0x" 
                           Foreground="White" 
                           FontSize="12"
                           VerticalAlignment="Center"
                           Margin="0,0,5,0"/>
                <ComboBox x:Name="SpeedComboBox" 
                          Width="60" 
                          Height="25" 
                          SelectionChanged="SpeedComboBox_SelectionChanged"
                          Background="#3a3a3a" 
                          Foreground="White"
                          BorderBrush="#555555">
                    <ComboBoxItem Content="0.5x" Tag="0.5"/>
                    <ComboBoxItem Content="0.75x" Tag="0.75"/>
                    <ComboBoxItem Content="1.0x" Tag="1.0" IsSelected="True"/>
                    <ComboBoxItem Content="1.25x" Tag="1.25"/>
                    <ComboBoxItem Content="1.5x" Tag="1.5"/>
                    <ComboBoxItem Content="2.0x" Tag="2.0"/>
                </ComboBox>
                <Button x:Name="MuteButton" 
                        Content="🔊" 
                        Width="35" 
                        Height="30" 
                        Margin="10,0,5,0"
                        Click="MuteButton_Click"
                        Background="Transparent" 
                        Foreground="White"
                        BorderBrush="Transparent"
                        FontSize="14"
                        Cursor="Hand"/>
                <Slider x:Name="VolumeSlider" 
                        Style="{StaticResource RoundedSliderStyle}"
                        Width="80" 
                        Minimum="0" 
                        Maximum="100" 
                        Value="100"
                        VerticalAlignment="Center"
                        ValueChanged="VolumeSlider_ValueChanged"/>
                <Button x:Name="FullScreenButton" 
                        Content="⛶" 
                        Width="35" 
                        Height="30" 
                        Margin="10,0,0,0"
                        Click="FullScreenButton_Click"
                        Background="Transparent" 
                        Foreground="White"
                        BorderBrush="Transparent"
                        FontSize="14"
                        Cursor="Hand"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
