<Window x:Class="DPlayer.App.Views.SubtitleSearchWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Style="{StaticResource DPlayerWindowStyle}"
        Title="Subtitle Search" Height="520" Width="720"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BackgroundBrush}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Search Subtitles" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,16"/>

        <!-- Search Form -->
        <Border Grid.Row="1" Style="{StaticResource PanelCardStyle}" Margin="0,0,0,12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,8,8">
                    <TextBlock Text="Title" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                    <TextBox Style="{StaticResource DPlayerTextBoxStyle}" Text="{Binding SearchTitle, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>
                <StackPanel Grid.Row="0" Grid.Column="1" Margin="8,0,0,8">
                    <TextBlock Text="Series" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                    <TextBox Style="{StaticResource DPlayerTextBoxStyle}" Text="{Binding SeriesTitle, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>
                <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,8,8">
                    <TextBlock Text="Season" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                    <TextBox Style="{StaticResource DPlayerTextBoxStyle}" Text="{Binding Season, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>
                <StackPanel Grid.Row="1" Grid.Column="1" Margin="8,0,0,8">
                    <TextBlock Text="Episode" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                    <TextBox Style="{StaticResource DPlayerTextBoxStyle}" Text="{Binding Episode, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>
                <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,0,8,0">
                    <TextBlock Text="Language" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                    <TextBox Style="{StaticResource DPlayerTextBoxStyle}" Text="{Binding LanguageCode, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>
                <Button Grid.Row="2" Grid.Column="1" Content="Search" Style="{StaticResource AccentButtonStyle}"
                        Command="{Binding SearchCommand}" VerticalAlignment="Bottom" HorizontalAlignment="Right"
                        Margin="8,0,0,0" Padding="24,8"/>
            </Grid>
        </Border>

        <!-- Results -->
        <ListBox Grid.Row="2" Style="{StaticResource DPlayerListBoxStyle}"
                 ItemContainerStyle="{StaticResource DPlayerListBoxItemStyle}"
                 ItemsSource="{Binding Results}" SelectedItem="{Binding SelectedResult}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="{Binding Title}" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding Release}" FontSize="12"
                                       Foreground="{StaticResource TextSecondaryBrush}"/>
                            <TextBlock Text="{Binding PreviewText}" FontSize="11" TextWrapping="Wrap"
                                       Foreground="{StaticResource TextSecondaryBrush}" MaxHeight="40"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="12,0,0,0">
                            <TextBlock Text="{Binding Language}" HorizontalAlignment="Right"
                                       Foreground="{StaticResource AccentBrush}"/>
                            <TextBlock HorizontalAlignment="Right" FontSize="12"
                                       Foreground="{StaticResource TextSecondaryBrush}">
                                <Run Text="★"/><Run Text="{Binding Rating, StringFormat={}{0:F1}}"/>
                            </TextBlock>
                            <TextBlock Text="{Binding Provider}" FontSize="10"
                                       Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Right"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Download Selected" Style="{StaticResource AccentButtonStyle}"
                    Command="{Binding DownloadSelectedCommand}" Margin="0,0,8,0"/>
            <Button Content="Close" Style="{StaticResource IconButtonStyle}"
                    Click="Close_Click" Width="Auto" Padding="16,8"/>
        </StackPanel>
    </Grid>
</Window>
